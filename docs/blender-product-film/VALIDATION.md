# 已验证范围

Skill 管线验证日期：2026-09-05。公开整理日期：2026-09-06。

- Skill 结构、Python 语法、JSON 与相对文档链接通过检查。
- 4 项轻量测试覆盖任务路径、场景合同、ZIP 路径与素材缓存/哈希/许可状态。
- macOS ARM64 上实际运行了原生三维构建、相机动画、Cycles CPU 渲染和 FFmpeg 编码/完整解码：256×256、24 fps、4 帧，仅用于验证管线。
- 同一配置再次渲染，新增帧数为 0，确认复用已校验缓存。
- smoke 输出被交付检查正确标为 partial；缺少真实 Armature 的反例也被拒绝。
- 实际双人骨架工程在多个动作帧通过取样检查：身体网格、骨架姿态和面部局部骨骼均有变化。
- 独立运行 MPFB 适配器，生成 19158 顶点人体、163 根骨骼、1 件服装和 4 个表情驱动。该适配器测试使用 bpy 4.5.3 与缓存素材。

这不是所有平台、GPU、人物资产和最终商业画质的全面认证。Windows/Linux 的安装分支、完整宣传片的具体美术以及外部后期链路，仍需在实际任务中验证。公开包不含原项目私有素材。封面是 ImageGen 概念视觉，不能作为上述测试的画面证据。
