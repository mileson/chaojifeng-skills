# <选题名> 发布包说明

- **项目名**：`<项目名>`
- **发布日期**：`<发布日期>`
- **版本**：`<版本号，如 v1>`
- **成片时长**：`<成片时长，如 02:44>`
- **分辨率/帧率**：`<如 1920×1080 / 30fps>`
- **封面模式**：`<attached_pic / first_frame / intro>`
- **字幕样式预设**：`<如 bilibili_white_heavy_outline>`

## 输出文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 最终成片 | `<项目名>_final.mp4` | 含硬字幕与封面元数据的发布版 MP4。 |
| 外挂 SRT 字幕 | `<项目名>.srt` | 纯文本字幕，供平台二次上传。 |
| 外挂 ASS 字幕 | `<项目名>.ass` | 样式化字幕，保留字体与描边信息。 |
| 剪辑决策 | `metadata/edit_decisions.json` | 原片到成片的时间轴映射、删除原因、置信度。 |
| 隐藏语音审查 | `metadata/hidden_speech_review.md` | 被删除或被文字修正的语音审查记录。 |
| 字幕分段计划 | `metadata/subtitle_plan.json` | 字幕事件、词级删除范围、文本级修正记录。 |
| 首帧校验图 | `check/check_first_frame.png` | 首帧画面目检。 |
| 字幕帧校验图 | `check/check_subtitle_frame.png` | 典型字幕呈现目检。 |
| 封面元数据校验图 | `check/check_cover_attachment.png` | attached_pic 模式下的封面提取校验。 |
| 特效叠加校验图 | `check/check_effect_frame.png` | hyperframes 关键帧目检。 |
| 发布说明 | `PUBLISH.md` | 本文件。 |

## 视频元数据

| 字段 | 值 |
|------|-----|
| 标题 | `<视频标题>` |
| 副标题 | `<副标题>` |
| 作者/署名 | `<来自 data/brand-profile.yaml>` |
| 平台标签 | `<标签1, 标签2, 标签3>` |
| 目标平台 | `<B站 / 视频号 / YouTube / 抖音 / 其他>` |
| 建议发布时间 | `<YYYY-MM-DD HH:MM>` |

## 封面处理

- **封面模式**：`<attached_pic / first_frame / intro>`
- **封面来源**：`<assets/cover.png / 用户上传 / AI 生成>`
- **封面说明**：`<一句话说明封面设计要点>`
- **附加信息**：
  - `attached_pic`：播放器列表显示封面，点击后从真实第一帧开始。
  - `first_frame`：封面图作为成片首帧停留 `<停留秒数>` 秒后切入正片。
  - `intro`：标题卡时长 `<标题卡秒数>` 秒，含标题与署名。

## 字幕与热词

- **字幕样式预设**：`<如 bilibili_white_heavy_outline>`
- **热词来源**：`data/corrections.yaml`
- **本次校正的关键替换**：
  - `<错误1>` → `<正确1>`
  - `<错误2>` → `<正确2>`
- **文本级修正说明**：`<如：仅修正 ASR 错别字与专有名词，未改动口语表达>`

## 剪辑摘要

- **原片时长**：`<原片时长>`
- **成片时长**：`<成片时长>`
- **删除总时长**：`<删除时长>`
- **剪辑强度**：`<conservative / standard / aggressive>`
- **主要删除类型**：
  - 长停顿：`<数量/时长>`
  - 口误/重复：`<数量/时长>`
  - 半截重说：`<数量/时长>`
- **语义剪辑是否经人工确认**：`<是 / 否 / 未进行语义剪辑>`

## 特效使用清单

| 节拍 | 时间戳 | 特效 ID | 用途说明 |
|------|--------|---------|----------|
| b1 | `<0:00:05-0:00:08>` | `<特效ID>` | `<用途>` |
| b2 | `<0:00:12-0:00:16>` | `<特效ID>` | `<用途>` |

## 校验结果

- [ ] 自动校验通过：`python3 scripts/validate_publish.py <publish_package_dir>`
- [ ] 首帧画面正常
- [ ] 字幕清晰可读、无遮挡
- [ ] 特效关键帧无穿帮
- [ ] 封面元数据/首帧/标题卡符合预期
- [ ] 音频无爆音、结尾自然
- [ ] 文件大小符合平台要求

## 发布平台与格式

| 平台 | 文件 | 说明 |
|------|------|------|
| `<平台1>` | `<项目名>_final.mp4` | `<如：横版 16:9，附带 SRT>` |
| `<平台2>` | `<项目名>_final.mp4` | `<如：竖版 9:16 衍生版>` |

## 诚实披露

- [ ] 口播稿已经本人口述定稿。
- [ ] ASR 使用词级时间戳进行精确剪辑。
- [ ] 自动剪辑中的语义删除已经人工确认。
- [ ] 所有 hyperframes 特效已逐关键帧目检。
- [ ] 封面图已经人工审核。
- [ ] 音频已完成响度归一化。
- [ ] 本平台未验证的部分：`<如无，请填写「无」；否则逐项列出>`。

## 变更记录

| 版本 | 日期 | 变更内容 | 操作人 |
|------|------|----------|--------|
| v1 | `<YYYY-MM-DD>` | 初始发布包生成。 | `<AI / 用户>` |
| v2 | `<YYYY-MM-DD>` | `<如：修正封面、调整字幕样式>` | `<AI / 用户>` |
